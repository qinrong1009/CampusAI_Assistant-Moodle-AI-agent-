# 校務系統 AI 助手 - 技術架構

## 🏗️ 系統架構

```
┌─────────────────────────────────────────────────────────────┐
│                      使用者瀏覽器                            │
├─────────────────────────────────────────────────────────────┤
│  ┌──────────────────────────────────────────────────────┐  │
│  │         Chrome Web Extension (前端)                  │  │
│  │  ┌──────────────┐  ┌──────────────┐  ┌──────────┐  │  │
│  │  │  Popup UI   │  │  Sidebar UI  │  │Content JS│  │  │
│  │  │  (設定面板) │  │  (互動面板) │  │(截圖)  │  │  │
│  │  └──────────────┘  └──────────────┘  └──────────┘  │  │
│  └──────────────────────┬───────────────────────────────┘  │
├─────────────────────────┼─────────────────────────────────┤
│                         │ HTTP/JSON                        │
│          API Gateway (chrome.runtime.sendMessage)          │
├─────────────────────────┼─────────────────────────────────┤
│                         ▼                                  │
│   ┌─────────────────────────────────────────────┐        │
│   │     Python Flask 後端 (localhost:5000)      │        │
│   │  ┌──────────────────────────────────────┐  │        │
│   │  │  API 路由層                          │  │        │
│   │  │  - /api/ask (主要端點)              │  │        │
│   │  │  - /api/models (模型查詢)           │  │        │
│   │  │  - /api/test (測試)                │  │        │
│   │  └──────────────────────────────────────┘  │        │
│   │  ┌──────────────────────────────────────┐  │        │
│   │  │  AI 模型層 (ai_model.py)             │  │        │
│   │  │  - Qwen 2.5 (推薦)                 │  │        │
│   │  │  - GPT-4V                          │  │        │
│   │  │  - Claude 3 Vision                 │  │        │
│   │  └──────────────────────────────────────┘  │        │
│   └─────────────────────────────────────────────┘        │
├──────────────────────────────────────────────────────────┤
│                    外部 API (網絡)                        │
│  ┌──────────┐    ┌──────────┐    ┌────────────┐        │
│  │阿里雲Qwen│    │ OpenAI  │    │ Anthropic │        │
│  │DashScope │    │ API     │    │ API       │        │
│  └──────────┘    └──────────┘    └────────────┘        │
└──────────────────────────────────────────────────────────┘
```

---

## 📁 項目結構

```
Capstone/
├── README.md                          # 項目總覽
├── QUICKSTART.md                      # 快速開始（5分鐘）
├── SETUP.md                           # 完整安裝指南
├── USAGE.md                           # 使用指南
├── DEVELOPMENT.md                     # 開發文檔
├── PROJECT_SUMMARY.md                 # 項目總結
├── ARCHITECTURE.md                    # 本文件
│
├── chrome-extension/                  # Chrome 擴展前端
│   ├── manifest.json                  # 擴展配置
│   └── src/
│       ├── html/
│       │   ├── popup.html              # 彈窗 UI
│       │   └── sidebar.html            # 側邊欄 UI
│       ├── css/
│       │   ├── popup.css               # 彈窗樣式
│       │   └── sidebar.css             # 側邊欄樣式
│       └── js/
│           ├── background.js           # 後台服務（API 中轉）
│           ├── content.js              # 內容腳本（截圖、DOM 操作）
│           ├── popup.js                # 彈窗交互邏輯
│           └── sidebar.js              # 側邊欄交互邏輯
│
├── backend/                           # Python Flask 後端
│   ├── app.py                         # 主應用文件
│   ├── config.py                      # 配置管理
│   ├── wsgi.py                        # WSGI 入口
│   ├── requirements.txt                # Python 依賴
│   ├── .env.example                   # 環境變數示例
│   ├── .env                           # 環境變數（本地）
│   └── app/
│       ├── __init__.py
│       ├── models/
│       │   ├── __init__.py
│       │   ├── ai_model.py             # AI 模型集成層
│       │   └── data_models.py          # 數據模型
│       └── routes/
│           ├── __init__.py
│           └── api_routes.py           # API 路由定義
│
├── run_dev.py                         # Python 啟動腳本
├── run_dev.sh                         # Linux/macOS 啟動腳本
├── run_dev.bat                        # Windows 啟動腳本
├── docker-compose.yml                 # Docker 容器編排
├── Dockerfile                         # Docker 構建配置
└── .gitignore                         # Git 忽略規則
```

---

## 🔄 數據流程

### 1️⃣ 用戶交互流程

```
用戶操作
    ↓
[快捷鍵: Alt+A 或點擊圖標]
    ↓
background.js 監聽命令
    ↓
content.js 在頁面中注入側邊欄 iframe
    ↓
sidebar.html 在 iframe 中加載
    ↓
用戶點擊「截圖」按鈕
    ↓
html2canvas 捕捉屁頁面
    ↓
截圖轉換為 base64 編碼
    ↓
用戶輸入問題並點擊「發送」
    ↓
content.js 發送訊息到 background.js
```

### 2️⃣ API 請求流程

```
background.js
    ↓
收到 Content Script 的訊息
    ↓
fetch() 發送 HTTP POST 請求
    ↓
    ├─ 目標: http://localhost:5000/api/ask
    ├─ 方法: POST
    └─ 數據:
       {
         "question": "用戶問題",
         "screenshot": "base64圖片",
         "model": "qwen/gpt/claude"
       }
    ↓
Flask 後端接收請求
    ↓
api_routes.py 的 ask_ai() 函數處理
    ↓
調用 AIModel.process_query()
    ↓
根據 model_type 選擇 AI 引擎:
    ├─ Qwen: _query_qwen()
    ├─ GPT: _query_gpt()
    └─ Claude: _query_claude()
    ↓
調用相應的外部 API
    ↓
獲取 AI 回應文本
    ↓
返回 JSON 響應
    ↓
background.js 收到回應
    ↓
content.js 顯示結果在 UI 中
```

### 3️⃣ 完整請求-響應周期

```
時間軸 (通常 2-5 秒)

0ms   |  用戶點擊「發送」
      ↓  顯示「加載中...」
100ms |  截圖已編碼為 base64
      ↓  HTTP POST 請求發出
150ms |  後端收到請求
      ↓  開始處理圖片和問題
200ms |  調用外部 AI API
      ↓  等待 AI 服務器響應...
2000ms|  AI 返回結果文本
      ↓  後端處理並返回 JSON
2050ms|  前端收到 JSON 響應
      ↓  隱藏「加載中」動畫
      ↓  在 UI 中顯示回應文本
2100ms|  用戶看到完整回應 ✅
```

---

## 🔐 數據安全

### 信息流向

```
校務系統頁面
    ↓
Chrome Extension (瀏覽器本地)
    ↓
[可選加密] → 本地後端 (Flask)
    ↓
[加密通信] → 外部 AI API (HTTPS)
```

### 隱私考量

1. **截圖處理**
   - 只在客戶端或您的服務器上處理
   - 不保存到云端（可選）
   - base64 編碼為安全格式

2. **API 密鑰**
   - 存儲在後端 `.env` 文件
   - 不經過瀏覽器傳輸
   - Chrome 擴展無法直接訪問

3. **問題和內容**
   - 發送到您配置的 AI 服務
   - 遵循對應服務的隱私政策
   - 可選部署到私有服務器

---

## 🛠️ 核心模塊詳解

### 1. Chrome Extension (前端)

**manifest.json**
- 定義擴展功能和權限
- 配置快捷鍵命令
- 指定 CSS 和 JS 資源

**background.js (Service Worker)**
- 監聽快捷鍵命令
- 中轉 Content Script 和後端的通信
- 管理 API 調用和存儲

**content.js**
- 在網頁中執行
- 注入 sidebar iframe
- 截圖功能（html2canvas）
- 與 iframe 通信

**sidebar.js (iframe 內)**
- UI 交互邏輯
- 提交表單處理
- 顯示 AI 回應

### 2. Flask 後端

**app.py (主應用)**
```python
- Flask 應用初始化
- CORS 配置
- 藍圖註冊
- 錯誤處理
```

**api_routes.py (API 路由)**
```python
- POST /api/ask → 主要處理端點
- GET /api/models → 列出可用模型
- GET /api/test → 測試端點
```

**ai_model.py (AI 集成層)**
```python
class AIModel:
    - process_query() → 分發到不同模型
    - _query_qwen() → Qwen API
    - _query_gpt() → OpenAI API
    - _query_claude() → Anthropic API
```

---

## 🚀 部署選項

### 選項 1: 本地開發（推薦開始）
```bash
後端: python run_dev.py  # localhost:5000
前端: Chrome 加載 chrome-extension/
```

### 選項 2: Docker 容器
```bash
docker-compose up
# 後端自動運行在 0.0.0.0:5000
```

### 選項 3: 遠程服務器
```bash
# 在服務器上
python app.py --host 0.0.0.0 --port 5000

# 在 Chrome 擴展設定中
API 網址: http://server-ip:5000
```

---

## 📊 性能指標

| 操作 | 通常耗時 | 最快 | 最慢 |
|------|---------|------|------|
| 截圖 | 500ms | 200ms | 1000ms |
| 編碼為 base64 | 100ms | 50ms | 200ms |
| 網絡傳輸 | 200ms | 50ms | 500ms |
| AI 處理 | 2000ms | 500ms | 5000ms |
| **總計** | **2.8s** | **0.8s** | **6.7s** |

---

## 🔧 可擴展性

### 添加新的 AI 模型

1. 在 `ai_model.py` 中添加新方法：
```python
def _query_new_model(self, question: str, image_data: bytes) -> str:
    # 實現
    pass
```

2. 在 `process_query()` 中添加分發：
```python
elif model_type == 'new_model':
    return self._query_new_model(question, image_data)
```

3. 在 `get_available_models()` 中註冊

### 添加新的 API 端點

1. 在 `api_routes.py` 中添加路由：
```python
@bp.route('/new-endpoint', methods=['POST'])
def new_endpoint():
    # 實現
    pass
```

2. 在前端 `content.js` 中調用

---

## 📝 API 文檔

### /api/ask (POST)

**請求：**
```json
{
  "question": "怎樣選課？",
  "screenshot": "data:image/jpeg;base64,...",
  "model": "qwen",
  "timestamp": "2025-10-31T10:30:00"
}
```

**響應 (200)：**
```json
{
  "status": "success",
  "response": "根據截圖...",
  "model": "qwen",
  "timestamp": "2025-10-31T10:30:02"
}
```

**錯誤 (500)：**
```json
{
  "error": "處理請求失敗",
  "message": "具體錯誤信息"
}
```

---

## 🎯 開發路線圖

- [x] 基礎架構搭建
- [x] Chrome 擴展開發
- [x] 後端 API 實現
- [x] AI 集成（Qwen/GPT/Claude）
- [ ] 錯誤日誌系統
- [ ] 性能監控
- [ ] 用戶反饋系統
- [ ] 多語言支持
- [ ] 高級功能（歷史記錄、收藏等）

---

**更新日期**: 2025-10-31  
**版本**: 1.0.0
